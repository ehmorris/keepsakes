= form_for :session, html: { class: 'standard-vertical' }, :url => session_path do |form|
  = form.text_field :email, type: 'email', placeholder: 'email'
  = form.password_field :password, placeholder: 'password'
  = form.submit
